# Docker

**Status: planned** ([../decisions/future-plans.md](../decisions/future-plans.md)).

> **TODO — not yet configured.**
> Once set up, document: install method (Debian repo vs docker.com), whether the
> daemon runs rootless or rootful, which users are in the `docker` group (note the
> security implication), and data-root location.
