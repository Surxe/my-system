# systemd services

> **TODO — not yet documented.**
> Notable custom units, timers, and enabled services.
> Enumerate with `systemctl list-unit-files --state=enabled` and
> `systemctl --user list-unit-files --state=enabled`.
