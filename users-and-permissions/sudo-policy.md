# Sudo policy

> **TODO — not yet documented.**
> Which users have sudo, any NOPASSWD rules, and drop-ins under
> `/etc/sudoers.d/`. Note the `dev` account is intended for automation, not
> interactive use ([users.md](users.md)) — document its sudo scope here.
