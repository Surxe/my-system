# SSH

- **SSH authentication is preferred** for git remotes and remote access.

> **TODO — expand.**
> Document configured hosts / `~/.ssh/config` aliases and key *types* (e.g.
> ed25519) as they're set up.
>
> **Never** record private keys, passphrases, or key material here — those are
> secrets ([../private/secrets-policy.md](../private/secrets-policy.md)).
